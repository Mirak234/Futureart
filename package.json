{
  "name": "future-art-mali-root",
  "private": true,
  "scripts": {
    "dev": "npm --prefix ./futurearts-website run dev",
    "build": "npm --prefix ./futurearts-website run build",
    "start": "npm --prefix ./futurearts-website run start",
    "lint": "npm --prefix ./futurearts-website run lint"
  }
}
